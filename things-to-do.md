Things to do:
1. change documentation so that it supports the package.json scripts instead for daemon.
daemon:start
daemon:stop
daemon:ping
ect...
2. Add functionality so that the package command adds a default toml to the package see [default.toml](config/default.toml)
3. make sure the python deamon command has all the same kind sub-commands as the node version.